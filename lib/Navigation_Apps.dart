import 'package:flutter/material.dart';

class navigationApps extends StatelessWidget {
  const navigationApps({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
